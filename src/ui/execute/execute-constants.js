// Shared constants for the Execute panel — imported by both index.jsx and ExecuteCanvas.jsx.
// Kept in a separate file so index.jsx never pulls in @xyflow/react at the module level.
export const DEFAULT_KPI_SLOTS = ["arrived", "served", "reneged", "waiting"];
