|                                                         |                                               |
| ------------------------------------------------------- | --------------------------------------------- |
| `queue(Triage Queue).length — entities in Triage Queue` | **Number of Patients in Triage Queue**        |
| `queue(Customer).length — customers waiting`            | What does this mean?                          |
| `idle(Nurse).count — idle servers`                      | **Number of available nurses**                |
| `busy(Nurse).count — busy servers`                      | Number of busy nurses                         |
| `attr(Nurse, serviceTime) — serviceTime of idle Nurse`  | **Nurse — serviceTime attribute**             |
| `served — cumulative customers served`                  | **Served — total who have completed service** |
| `reneged — cumulative customers reneged`                | **Reneged — total who abandoned the queue**   |