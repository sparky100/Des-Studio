
A queue name cannot be changed!
Trying to change the name of event automatically prompts to save changes - this happens as typing rather than when edit is complete
editing and saving object names is not working properly
Changing an entity name isn't reflected through the model e.g conditions disappear


add a traffic light system which shows where the app is ready to run. add a traffic light system which shows where the app is ready to run
Only show a validation panel if issues exist
The menu options are doing words, what should we use instead of Results
Can we change the font on the overview screen text boxes
What do undo and redo do?
Ai designer should be accessed from the design menu not the top menu
The visual design panel is refreshing and resizing too much as I edit a diagram
On the design screen is the validation panel and workflow context saying the same thing?
In fact there are 3 lots of validation information
Export model could come under access or sharing
Need to reword scalar state variables to be more user friendly
Possibly need a check that there is no wording that is too technical 
Perhaps validate should just be renamed model health as it doesn't let a user validate it shows the results of the model validation
The validation workspace has a run model button - that should be removed as we already have a button for execute
Phase C scan hit the 500-pass cap — model may have an unstable or conflicting C-event condition
Check your C-event conditions for cycles or conditions that never become false. Picked at run time not by validation
